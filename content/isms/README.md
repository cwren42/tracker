# Managed ISMS Content Area

This directory is reserved for managed ISMS content outside the Jinja templates tree.

Suggested layout:

- `incoming/` for new manual source files waiting to be imported
- future published/manual assembly output directories as the module expands

Phase 1 uses the database as the active source for imported document metadata and versions.