psutil>=5.9.0
requests>=2.28.0
websockets>=10.0
# urllib3 is imported directly (to silence self-signed-cert InsecureRequestWarning);
# it ships transitively with requests but is pinned here to match the actual imports.
urllib3>=1.26.0
